# Dependencies for Streamlit Community Cloud (pip, CPU-only).
# Local development uses uv + pyproject.toml instead; this file exists so
# Streamlit Cloud installs the CPU torch build (its runners have no GPU).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0
torchaudio==2.11.0
transformers==5.15.1
librosa==1.0.0
soundfile==0.14.0
numpy
streamlit
